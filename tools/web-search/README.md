# Web search tool

This directory owns the planned deterministic `web-search-tool` executable and
its Nix package definition. It will retrieve normalized search results while
preserving source provenance; research synthesis remains an agent concern.

Implementation begins in Milestone 4. Milestone 1 defines only the component
boundary.
