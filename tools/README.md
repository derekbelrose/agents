# Tools

Tools are independently executable, deterministic capabilities. Each tool owns
its input validation and normalized output, but does not interpret the user's
larger goal or synthesize research.

Milestone 4 provides `web-search-tool`, a Brave-backed search capability with
human, JSON, and manifest interfaces. See `tools/web-search/README.md`.
