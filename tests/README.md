# Tests

This directory will hold cross-component and executable-contract tests. Normal
checks must remain deterministic and must not require live LLM or Internet
access.

Milestone 1 repository validation is defined in `flake.nix`.
